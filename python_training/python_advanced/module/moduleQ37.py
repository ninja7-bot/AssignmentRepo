"""
Create a package for mathematical operations (add, subtract, multiply, divide) and use it.
"""

from maths.add import *
from maths.mul import *
from maths.div import *
from maths.sub import *

a = 10
b = 5

print(add(a, b))
print(sub(a, b))
print(mul(a, b))
print(div(a, b))